<nav class="navbar navbar-expand-lg bg-black fixed-top py-2 shadow border-0">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_acceuil') }}">
            <img src="{{ asset('img/iconenavbar.png') }}" alt="Logo" height="40" class="d-inline-block align-text-top">
        </a>
        <button class="navbar-toggler " style="background-color: darkorange" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent"
                aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon "></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mx-auto mb-2 mb-lg-0 gap-5">
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle bg-warning text-black fw-bold rounded px-3 py-2 hover-invert fs-5" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">Sorties</a>
                        <ul class="dropdown-menu shadow border-0 mt-2">
                            <li><a class="dropdown-item fw-bold hover-invert" href="{{ path('sortie_list') }}">Les sorties disponibles</a></li>
                            <li><a class="dropdown-item fw-bold hover-invert" href="{{ path('sortie_create') }}">Créer une sortie</a></li>
                            <li><a class="dropdown-item fw-bold hover-invert" href="{{ path('sortie_cancelled') }}">Sorties annulées</a></li>
                            <li><a class="dropdown-item fw-bold hover-invert" href="#">Sorties archivées</a></li>
                        </ul>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle bg-warning text-black fw-bold rounded px-3 py-2 hover-invert fs-5" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">Lieux</a>
                        <ul class="dropdown-menu shadow border-0 mt-2">
                            <li><a class="dropdown-item fw-bold hover-invert" href="{{ path('lieu_list') }}">Les lieux disponibles</a></li>
                            <li><a class="dropdown-item fw-bold hover-invert" href="{{ path('lieu_create') }}">Créer un lieu</a></li>
                        </ul>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle bg-warning text-black fw-bold rounded px-3 py-2 hover-invert fs-5" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">Villes</a>
                        <ul class="dropdown-menu shadow border-0 mt-2">
                            <li><a class="dropdown-item fw-bold hover-invert" href="{{ path('ville_list') }}">Les villes disponibles</a></li>
                            <li><a class="dropdown-item fw-bold hover-invert" href="{{ path('ville_create') }}">Créer une ville</a></li>
                        </ul>
                    </li>
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle bg-warning text-black fw-bold rounded px-3 py-2 hover-invert fs-5" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">Admin</a>
                            <ul class="dropdown-menu shadow border-0 mt-2">
                                <li><a class="dropdown-item fw-bold hover-invert" href="{{ path('app_register') }}">Enregistrer un participant</a></li>
                                <li><a class="dropdown-item fw-bold hover-invert" href="{{ path('list_users') }}">Liste des participants</a></li>
                                <li><a class="dropdown-item fw-bold hover-invert" href="{{ path('import_participants') }}">Import de participants par CSV</a></li>
                            </ul>
                        </li>
                    {% endif %}
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link border fw-bold rounded px-3 py-2 hover-invert" style="color: darkorange" aria-current="page" href="{{ path('app_login') }}" title="Se connecter">Se connecter</a>
                    </li>
                {% endif %}
            </ul>
            {% if app.user %}
                <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                    <li class="nav-item dropdown d-flex align-items-center">
                        <!-- Photo de profil -->

                        <a class="nav-link dropdown-toggle bg-warning text-black fw-bold rounded px-3 py-2 hover-invert fs-5" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">Profil</a>
                        <ul class="dropdown-menu dropdown-menu-end shadow border-0 mt-2">
                            <li><a class="dropdown-item fw-bold hover-invert" href="{{ path('mon_profil') }}">Mon profil</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item fw-bold hover-invert" href="{{ path('app_logout') }}">Se déconnecter</a></li>
                        </ul>
                        <div class="me-2">
                            {% if app.user.photoProfile %}
                                <img src="{{ asset('img/photoDeProfil/' ~ app.user.photoProfile) }}" class="rounded-circle" style="border: 5px solid darkorange;width: 100px; height: 100px; object-fit: cover;"/>
                            {% else %}
                                <div class="bg-secondary rounded-circle d-flex align-items-center justify-content-center" style="width: 40px; height: 40px;">
                                    <i class="bi bi-person-fill text-white"></i>
                                </div>
                            {% endif %}
                        </div>
                    </li>
                </ul>
            {% endif %}
        </div>
    </div>
</nav>

<!-- Ajoutez cette balise script pour inclure Bootstrap Icons via CDN -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
